#include "main.h"
/**
 * _islower - A function that checks for lowercase character.
 * @lowerCase : This checks the case of the character
 * Return: Always 0 (Success)
 */
int _islower(int lowerCase)
{
return (lowerCase >= 97 && lowerCase <= 122);
}
